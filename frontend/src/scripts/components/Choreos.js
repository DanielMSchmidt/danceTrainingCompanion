'use strict';

var React = require('react/addons');

require('styles/Choreos.sass');

var Choreos = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for Choreos</p>
        </div>
      );
  }
});

module.exports = Choreos; 

