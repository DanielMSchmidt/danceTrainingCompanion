'use strict';

var React = require('react/addons');

require('styles/LatestActivity.sass');

var LatestActivity = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for LatestActivity</p>
        </div>
      );
  }
});

module.exports = LatestActivity; 

